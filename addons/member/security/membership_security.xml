<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="group_manage_membership" model="res.groups">
        <field name="name">Manage memberships</field>
            <field name="category_id" ref="base.module_category_dvzo_membership"/>
        <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        <field name="comment">The user will be able to add, edit and delete memberships.</field>
    </record>

</odoo>
